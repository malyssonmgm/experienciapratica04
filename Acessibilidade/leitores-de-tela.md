# Suporte para Leitores de Tela

- Uso de `aria-label` em botões e links
- Imagens com `alt` descritivo
- Estrutura semântica com `<main>`, `<nav>`, `<section>`, `<footer>`
- Elementos interativos com `role` e `aria-describedby` quando necessário